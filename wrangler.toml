name = "security-hf"
main = "worker.ts"
compatibility_date = "2024-01-01"

# Environment variables (secrets should be set via: wrangler secret put HF_TOKEN)
# [vars]
# Add non-secret environment variables here if needed

# To set your Hugging Face token as a secret:
# Run: npx wrangler secret put HF_TOKEN
# Then paste your token when prompted
